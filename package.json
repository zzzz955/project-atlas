{
  "name": "project-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "project-atlas — C++ MMO game-server framework. Node data-pipeline tooling (pkt/db generators; info_generator lands with the demo-game CSVs in a later slice).",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "gen:db": "node tools/db_generator/db_generator.js",
    "gen:db:check": "node tools/db_generator/db_generator.js --check",
    "gen:pkt": "node tools/pkt_generator/pkt_generator.js",
    "gen:pkt:check": "node tools/pkt_generator/pkt_generator.js --check",
    "gen:all": "npm run gen:db && npm run gen:pkt",
    "gen:check": "npm run gen:db:check && npm run gen:pkt:check",
    "check:core-purity": "node tools/core_purity/core_purity.js"
  }
}

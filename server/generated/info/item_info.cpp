// AUTO-GENERATED by tools/info_generator/info_generator.js — DO NOT EDIT.
// 🔴 Hand edits are silently lost on the next `npm run gen:info` and `npm run gen:info:check`
//    fails the CI gate. Change the CSV under shared/datas/ instead.
// source: shared/datas/item.csv
//
// The data itself. 🔴 It lives in the .cpp rather than the header so that one translation unit
//    holds one copy of the table however many places look it up, and so that the unity-OFF build
//    (architecture-design.md §15.1) has a TU here to prove the header is self-contained.

#include "generated/info/item_info.h"

#include <algorithm>
#include <array>
#include <span>

namespace atlas::generated {

namespace {

constexpr std::array<ItemInfoRow, kItemInfoRowCount> kRows = {{
    {.id_ = 1001, .slot_ = 1},
    {.id_ = 1002, .slot_ = 1},
    {.id_ = 2001, .slot_ = 2},
    {.id_ = 3001, .slot_ = 3},
}};

}  // namespace

std::span<const ItemInfoRow> ItemInfoRows() noexcept { return kRows; }

// 🔴 Binary search, not a scan: the generator emits the rows sorted by primary key, so this
//    stays O(log n) when a real game brings thousands of rows and the call sites do not change.
const ItemInfoRow* FindItemInfo(UInt32 id) noexcept {
    const auto hit = std::lower_bound(
        kRows.begin(), kRows.end(), id,
        [](const ItemInfoRow& row, UInt32 key) { return row.id_ < key; });
    if (hit == kRows.end() || hit->id_ != id) {
        return nullptr;
    }
    return &*hit;
}

}  // namespace atlas::generated
